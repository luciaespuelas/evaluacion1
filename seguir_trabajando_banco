#blablabla
print("cuanto dinero quieres meter?")
num=int(input())
def banco_() :
    while True:
        print ("cuanto dinero quieres sacar")
        sacar=int(input())
        if (sacar)==0:
            print ("no has sacado dinero")
            print (num)
        else:
            if (sacar%5==0) :
                print ("dsd")
banco_()



----------------------------------------------------
def banco_() :
    print ("cuantodinero quieres meter? ")
    num=int(input())
    while True:
        print ("cuanto quieres sacar? ")
        sacar=int(input())
        if (sacar)==0 :
            print ("no has sacado dinero")
            print (num)
        else:
            if (sacar%5==0) :
                resto= (num-sacar)
                print (resto)
                print ("seguir?")
                si=input()
                if (si==si):
                    print ("AA")
                else:
                    print ("Dsd")
            else:
                print ("error")
    print ("seguir")
  
    
banco_()

------------------------------------------------- ESTE
def banco_() :
    num=0
    print ("cuantodinero quieres meter? ")
    num=int(input())
    while True:
        print ("cuanto quieres sacar? ")
        sacar=int(input())
        if (sacar)==0 :
            print ("no has sacado dinero")
            print (num)
        else:
            if (sacar%5==0) :
                resto= (num-sacar)
                print (resto)
                print ("seguir?")
                si=input()
                if (si=="si") :
                    print ("AA")
                    continue
                else:
                    print ("Dsd")
                    break
            else:
                print ("error")
    print ("end")
  
    
banco_()

----------------------------------------- FUNCIONA
def banco_() :
    num=0
    print ("cuanto dinero quieres meter? ")
    num=int(input())
    while True:
        print ("cuanto dinero quieres sacar? ")
        sacar=int(input())
        if (sacar)==0 :
            print ("no has sacado dinero")
            print (num)
        else:
            if (sacar%5==0) :
                resto= (num-sacar)
                print (resto)
                num=(resto)
                print ("seguir?")
                si=input()
                if (si=="si") :
                    print ("bien")
                    continue
                else:
                    break
            else:
                print ("error")
    print ("end")
  
    
banco_()

--------------------------------------------------- DEFINITIVO
def banco_() :
    num=0
    print ("cuanto dinero quieres meter? ")
    num=int(input())
    while True:
        print ("cuanto quieres sacar? ")
        sacar=int(input())
        if ((num-sacar) <0) :
            print ("no se puede sacrar dinero")
            print (num)
            break
        else:
            if (sacar)==0 :
                print ("no has sacado dinero")
                print (num)
            else:
                if (sacar%5==0) :
                    resto= (num-sacar)
                    print (resto)
                    num=(resto)
                    print ("seguir?")
                    si=input()
                    if (si=="si") :
                        print ("bien")
                        continue
                    else:
                        break
                else:
                    print ("error")
        print ("end")
  
    
banco_()
