#escribimos la tabla que se pida
def tabla1_():
    ntbl=int(input("la tabla del:"))
    for cont in range(1,11):
        print (str("multp ") + str(ntbl)+ "x" + str(int(cont)) + "=" + str(ntbl*cont))
    
    
tabla1_()
